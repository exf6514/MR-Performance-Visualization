<UserControl x:Class="MR_Performance_Visualization.UserControlMemoryUsage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MR_Performance_Visualization"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="545" d:DesignWidth="774">
    <Grid Background="White">
        <materialDesign:TransitioningContent x:Name="TrainsitionigContentSlide" OpeningEffect="{materialDesign:TransitionEffect SlideInFromLeft, Duration=0:0:0.8}" Panel.ZIndex="-5">
            <StackPanel>
                <Grid Height="45" VerticalAlignment="Top">
                    <Border BorderThickness="0 0 0 1" BorderBrush="black">
                        <TextBox Margin="10,0,10,0" Text="Memory Usage" FontSize="17" FontWeight="Bold" VerticalAlignment="Bottom" BorderBrush="{x:Null}"/>
                    </Border>
                </Grid>
                <StackPanel Margin="25 0">
                    <Grid Height="150" Margin="0 30 0 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" Width="250" Margin="0 20">
                            <Grid.Effect>
                                <DropShadowEffect BlurRadius="20" Color="#FFDEDEDE" ShadowDepth="1"/>
                            </Grid.Effect>
                            <Border Background="GhostWhite" BorderBrush="Silver" BorderThickness="1" CornerRadius="8,8,3,3">
                                <StackPanel>
                                    <Border Background="Red" BorderBrush="Silver" BorderThickness="1" CornerRadius="8,8,0,0">
                                        <TextBlock Padding="10" FontSize="18" Text="HIGHEST per CPU" Foreground="White" TextAlignment="Center"/>
                                    </Border>
                                    <TextBlock FontSize="26" FontWeight="Bold" TextAlignment="Center" Padding="10">96%</TextBlock>
                                </StackPanel>
                            </Border>
                        </Grid>
                        <Grid Grid.Column="1" Background="#FFF3F3F3" Width="250" Margin="0 20">
                            <Grid.Effect>
                                <DropShadowEffect BlurRadius="20" Color="#FFDEDEDE" ShadowDepth="1"/>
                            </Grid.Effect>
                            <Border Background="GhostWhite" BorderBrush="Silver" BorderThickness="1" CornerRadius="8,8,3,3">
                                <StackPanel>
                                    <Border Background="Green" BorderBrush="Silver" BorderThickness="1" CornerRadius="8,8,0,0">
                                        <TextBlock Padding="10" FontSize="18" Text="LOWEST per CPU" Foreground="White" TextAlignment="Center"/>
                                    </Border>
                                    <TextBlock FontSize="26" FontWeight="Bold" TextAlignment="Center" Padding="10">31%</TextBlock>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>
                    <Grid Height="300">
                        <Grid x:Name="MainContentGrid" HorizontalAlignment="Center" VerticalAlignment="Center" Height="250" Width="650">
                            <lvc:CartesianChart Series="{Binding SeriesCollection}" Margin="-25,0,-25,0" />
                        </Grid>
                    </Grid>
                </StackPanel>
            </StackPanel>
        </materialDesign:TransitioningContent>
    </Grid>
</UserControl>
